#pragma once
// Copy this file to secrets_esp32.h and fill in your values.
// secrets_esp32.h is gitignored — never commit the real file.

#define WIFI_SSID     "your_wifi_ssid"
#define WIFI_PASSWORD "your_wifi_password"

// IP of the desktop running the Node server (audioIngest unit 2, TCP :8002)
#define STREAM_HOST   "192.168.1.100"
